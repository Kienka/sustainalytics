

"""
Responsible for loading the data and deciding what events should be applied to it. i.e skiprows
This uses pandas collection format but automatically decided
"""
import pandas as pd

def load_data(file):
    """
    Loads the data in a correct format and make it available as a dataframe
    :param file:
    :return: a data frame
    """

def process_data_file(file):
    """
    Detects the file type and assign the right format for the
    :param file:
    :return:
    """